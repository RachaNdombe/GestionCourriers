namespace JconsultGC.Models;

public static class SeedData
{
    // Helper for seeding initial data -- call from a migration or startup if needed.
    public static void Initialize(IServiceProvider serviceProvider)
    {
        // Implementation intentionally minimal here. Use migrations or custom seed logic.
    }
}
